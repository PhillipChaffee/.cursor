---
description: Template — fill in with your own voice before enabling.
alwaysApply: false
---

# Writing Voice (template)

This is a **fill-in template**. Replace every `<...>` placeholder with your own preferences, then set `alwaysApply: true` (or enable the rule in Cursor Settings) when you are ready to use it.

This rule governs MR comments, code review feedback, discussion replies, Slack messages, and research/verification writeups. MR descriptions, design docs, tickets, and code comments/docstrings usually have their own structural rules — say here whether this voice applies to them or not: `<scope note>`.

## Clarity principles

- `<what every piece of writing must do for the reader>`
- `<how you lead vs bury the point>`
- `<what to do when you are unsure of the claim>`

## Capitalization & punctuation

- `<sentence capitalization defaults>`
- `<how you treat the pronoun "i">`
- `<period / question mark / ellipsis habits>`
- `<rules for dashes, semicolons, colons>`

## Abbreviations & slang

- `<list abbreviations you use and when>`
- `<when to stay formal vs informal>`

## Tone

- `<default tone>`
- `<how you disagree>`
- `<how you hedge>`
- `<how you credit agent/automated work>`

### Worked example (fictional — replace with yours)

> Tone: casual and direct, like texting a trusted coworker. Hedge opinions with "i think" / "seems like". Never harsh. Credit agents explicitly ("i had the scanner check…").

## Pronouns

- `<when you use "i" vs "we">`

## Feedback patterns

- `<how you ask why>`
- `<how you make suggestions vs stronger opinions>`
- `<how you end reviews (approve / request changes)>`

## Comment length

- `<short vs medium vs long norms>`
- `<Slack: one thought per message?>`

## Research / verification writeups

- `<opener pattern>`
- `<section header style>`
- `<bullet rules>`
- `<when to use horizontal rules>`

## Characteristic phrases

- `<phrase 1>`
- `<phrase 2>`
- `<phrase 3>`

## Examples

Line-level review:
- `<example>`

MR-level feedback:
- `<example>`

Quick replies:
- `<example>`
